{{#if errorMessage}}
<div class="error-box">
    <p>{{errorMessage}}</p>
</div>
{{/if}}
<form class="search" method="post">
    <input type="search" name="search" value="" placeholder="Search email...">
    <button type="submit" class="btn-search">
        <!-- Do not forget to change the path to the image -->
        <img src="./static/img/search-icon.png" alt="search-icon">
        Search
    </button>
</form>

{{#if searched}}
<section id="search-page">
    <h2>Search result:</h2>
    {{#if searchResults}}
    <!--If there are a search matches, show each ad-->
    {{#each searchResults}}
    <article class="search-page-result">
        <p class="search-page-result-headline">Headline: {{searchResults.headline}}</p>
        <p class="search-page-result-companyName">Company Name: {{searchResults.companyName}}</p>
    </article>
    {{/each}}
    {{else}}
    <!--If not show:-->
    <article class="search-page-no-result">
        <p class="search-page-no-result-message">No match found.</p>
    </article>
    {{/if}}
</section>
{{/if}}